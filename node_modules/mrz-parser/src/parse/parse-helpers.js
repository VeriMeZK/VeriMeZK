const eqField = (f, v) => f.field === v;

const isDocNumber = (f) => eqField(f, 'documentNumber');
const isSex = (f) => eqField(f, 'sex');
const isBirth = (f) => eqField(f, 'birthDate');

module.exports = {
  eqField,
  isDocNumber,
  isSex,
  isBirth,
};
