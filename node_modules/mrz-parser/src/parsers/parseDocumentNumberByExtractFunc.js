const parseDocumentNumber = require('./parseDocumentNumber');

module.exports = (extractFunc) => (source, checkDigit, optional) => parseDocumentNumber(
  ...extractFunc(source, checkDigit, optional),
);
