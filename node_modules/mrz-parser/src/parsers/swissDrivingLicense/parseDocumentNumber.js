module.exports = function parseDocumentNumber(source) {
  // swiss driving license number
  if (!source.match(/^[0-9]+$/)) {
    throw new Error(
      `invalid document number: ${source}. It must contains digits only`,
    );
  }
  return {
    value: source,
    start: 5,
    end: 17,
  };
};
